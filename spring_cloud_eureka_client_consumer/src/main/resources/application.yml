eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${eureka.instance.appname}:${server.port}
    appname: client_consumer

server:
  port: 8890

